namespace WC2_Save_Editor
{
    class GameResources
    {
        // New gold value and it's offset depending on the campaign type (orc/ human)
        public int goldOffset, gold;

        // New wood value and it's offset depending on the campaign type (orc/ human)
        public int woodOffset, wood;

        // New oil value and it's offset depending on the campaign type (orc/ human)
        public int oilOffset, oil;
    }
}
